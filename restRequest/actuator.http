### actuator
GET http://localhost:{{application-port}}/actuator
Accept: application/json

### actuator info
< {%
    function randomHex(length) {
        let result = '';
        const chars = '0123456789abcdef';
        for (let i = 0; i < length; i++) {
            result += chars[Math.floor(Math.random() * chars.length)];
        }
        return result;
    }

    const traceId = randomHex(32);
    const spanId  = randomHex(16);

    request.variables.set("traceId", traceId);
    request.variables.set("spanId", spanId);
%}
GET http://localhost:{{application-port}}/actuator/info
Accept: application/json
traceparent: 00-{{traceId}}-{{spanId}}-01
baggage: testBaggage=hallo

### actuator health
GET http://localhost:{{application-port}}/actuator/health
Accept: application/json

### actuator health
GET http://localhost:{{application-port}}/actuator/health/readiness
Accept: application/json

### actuator health
GET http://localhost:{{application-port}}/actuator/health/liveness
Accept: application/json

### actuator sbom
GET http://localhost:{{application-port}}/actuator/sbom
Accept: application/json

### actuator sbom application
GET http://localhost:{{application-port}}/actuator/sbom/application
Accept: application/octet-stream

### actuator metrics
GET localhost:{{application-port}}/actuator/metrics
Accept: application/json

### actuator env
GET localhost:{{application-port}}/actuator/env
Accept: application/json

### actuator env
GET localhost:{{application-port}}/actuator/configprops
Accept: application/json

### actuator beans
GET localhost:{{application-port}}/actuator/beans
Accept: application/json

### actuator caches
GET localhost:{{application-port}}/actuator/caches
Accept: application/json

### actuator get loggers
GET localhost:{{application-port}}/actuator/loggers

### Get Prometheus Metrics
GET http://localhost:{{application-port}}/actuator/prometheus
Accept: application/openmetrics-text;version=1.0.0;charset=utf-8

### Get Prometheus Metrics
GET http://localhost:{{application-port}}/actuator/prometheus
Accept: text/plain;version=0.0.4;charset=utf-8
